---
import MainLayout from "../../layouts/MainLayout.astro";

const productId = Astro.url.pathname.split("/").pop();
const product = {
  id: productId,
  name: "Producto Ejemplo",
  description: "Descripción del producto",
  price: 19.99,
  store: "1",
};
---

<MainLayout>
  <h1>{product.name}</h1>
  <p>{product.description}</p>
  <p>Precio: ${product.price}</p>
  <p>Tienda: <a href={`/store/${product.store}`}>{product.store}</a></p>
</MainLayout>
